"""Streamlit UI package: a thin, in-process front end over the pipeline
functions in mmss.pipeline -- no HTTP layer, run directly via
`streamlit run src/mmss/ui/app.py` (see app.py for the reasoning)."""
